import React from 'react';
import { connect } from 'react-redux';

const Admin_ = ({ user }) => {
    return null;
};

const mapStateToProps = state => {
    return {
        
    };
};

const mapDispatchToProps = dispatch => {
    return {

    };
};

export default connect(mapStateToProps, mapDispatchToProps)(Admin_);